import NamingStrategy from "./NamingStrategy";

export default class SnakeCaseNamingStrategy implements NamingStrategy {
    translate(input: String): String {
        throw new Error("Method not implemented.");
    }
}